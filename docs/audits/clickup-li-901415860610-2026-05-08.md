# ClickUp Audit — List li/901415860610 — 2026-05-08

Source: https://app.clickup.com/90141182095/v/b/li/901415860610  
Fetched via ClickUp API using local `.env.clickup` token.

## List metadata

- **List:** `02 — Infra Build (Keeper)` (`901415860610`)
- **Folder:** `Twlv20 AI OS — Phase 1` (`90148921529`)
- **Space:** `Space` (`90145300556`)
- **List description:** The §8 infra task package — Plinko writes specs + acceptance criteria + review gate, Keeper executes. Same HITL pattern the live system will run on. Burn-down target: end of S2 (Week 1).
- **Tasks fetched:** 8
- **Custom fields:** none present on fetched tasks
- **Subtasks:** none present on fetched tasks
- **Attachments:** none present on fetched tasks
- **Comments:** 2 total across fetched tasks

## Repository cross-reference

Checked expected repo locations:

- `skills/` — **missing**
- `specs/` — **missing**
- `docs/RUNBOOKS/` — **missing**

Relevant existing repo artifacts:

- `sources/Infra Build Steps for Phase 2.txt` — source spec for this list/task package.
- `sources/Copy of Twlv20_AI_OS___Architecture_One-Pager_v1.md.txt` — architecture/context describing ClickUp HITL, tenants, and Keeper execution model.
- `reports/clickup/phase1_snapshot.json` — prior ClickUp snapshot.
- `reports/clickup/phase1_audit.md` — prior task inventory.
- `integrations/clickup/README.md`, `integrations/clickup/CONFIG.md`, `scripts/clickup.py` — ClickUp API integration helper.

Major repo gap: the task list assumes a future `twlv20-ai-skills/` repo with `infra/schema.sql`, `infra/migrations/`, `runtime/`, `skills/`, and tests, but the current workspace only contains source docs and ClickUp scripts. No executable skills/specs/runbooks exist yet in the expected paths.

## A. Summary

The ClickUp list contains 8 tasks. **0/8 are fully actionable as-is (0%)** under the requested criteria because every execution task either depends on missing production prerequisites, missing repo artifacts, missing credentials/human decisions, or represents production infrastructure change requiring explicit approval and safer access handling. The top blockers are: no `skills/`, `specs/`, or `docs/RUNBOOKS/` in this repo; missing `infra/schema.sql`/migration files; unresolved droplet/IP/domain/SSH-key details; missing GitHub/private repo access and deployment target; missing Infisical/B2/email credentials; and exposed root/password material in ClickUp that should be rotated before further work.

## B. CAN DO

| task | tenant | proposed skill/path | est. effort |
|---|---:|---|---:|
| None — no task is fully specified with dependencies met and safe production access available. | n/a | n/a | n/a |

## C. CANNOT DO

| task | blocker | what's needed to unblock |
|---|---|---|
| `02 — Infra Build (Keeper) — Hardened checklist` | Duplicates/overlaps `Droplet hardening`; not a distinct execution unit. Missing selected canonical droplet IP/domain, SSH public keys, and approval for production hardening. | Decide whether to archive this as a checklist/reference or merge into `Droplet hardening`; confirm canonical IP/domain/keys; approve production changes. |
| `Droplet Access` | Contains sensitive credentials and conflicting access details (`162.243.252.92` in description vs `137.184.27.61` in hardening comment). This is credential material, not an executable build task. | Confirm canonical droplet; rotate exposed root/app passwords/API token after access is stabilized; replace password access with named SSH keys; move secrets to Infisical/password manager. |
| `Droplet hardening — Caddy/TLS, SSH-key-only, fail2ban` | Production/security changes; missing canonical hostname/domain, DNS confirmation, Keeper/Jon SSH public keys, and explicit approval. Also blocked by exposed root password requiring rotation. | Confirm droplet IP, hostname/domain/subdomains, DNS records, SSH public keys, and approval window; rotate credentials; then execute hardening. |
| `Postgres 16 + pgvector install` | Explicitly depends on droplet hardening. Requires production DB installation and credential choices. | Complete/approve droplet hardening; confirm DB names/roles/password storage path; approve installation. |
| `Tenant schema + RLS migration (per Plinko spec)` | Depends on Postgres and missing `infra/schema.sql` / versioned migration files. Current repo has prose spec only, no SQL artifact. | Create/approve `infra/schema.sql`, migrations, and RLS isolation test; complete Postgres install; confirm tenant UUIDs/names. |
| `Infisical — self-hosted install + per-tenant scoping` | Depends on hardened droplet and Postgres/Redis. Missing domain (`infisical.<domain>`), SMTP/email settings, encryption-key custody decision, tenant machine-identity spec, and migration plan for current secrets. | Complete droplet + Postgres; confirm domain/DNS, SMTP, offline backup owner/location, tenant/project naming, and secrets migration source of truth. |
| `Weekly encrypted Postgres snapshots → S3 / Backblaze B2` | Depends on Postgres and Infisical. Missing B2/S3 bucket credentials, GPG passphrase custody, alert email/SMTP, retention policy confirmation, and scratch restore host. | Complete Postgres + Infisical; provision B2/S3 target; approve passphrase storage/offline copy; confirm alert destination and restore-drill host. |
| `GitHub repo scaffold + CI + deploy hook (twlv20-ai-skills)` | Requires private GitHub repo/org access, deploy key or GitHub Actions secret, branch protection/admin rights, runtime/service name, and deploy command. Current workspace is not the requested private repo scaffold. | Confirm/create `twlv20-ai-skills` repo, grant access, define runtime/systemd service/deploy path, add required secrets, and approve branch protection/deploy workflow. |

## D. NEEDS CLARIFICATION

### `02 — Infra Build (Keeper) — Hardened checklist`
- Should this remain as a parent/reference checklist, or should it be closed/merged into `Droplet hardening`?
- Which task is the canonical source of truth for hardening acceptance criteria?

### `Droplet Access`
- Which IP is current/canonical: `162.243.252.92` or `137.184.27.61`?
- Has the exposed root password already been rotated?
- Should this task be converted into a private credential-handoff note and removed from normal ClickUp task descriptions?
- What is the correct dashboard tunnel/port mapping? The description forwards `18789:127.0.0.1:18788` but opens `localhost:18788`.

### `Droplet hardening — Caddy/TLS, SSH-key-only, fail2ban`
- What domain should Caddy serve? Suggested subdomains are listed, but no actual domain is specified.
- Are DNS A records already pointed at the droplet?
- What are Keeper and Jon's SSH public keys?
- Should root SSH be disabled immediately after named users are verified?
- What maintenance window is acceptable for firewall/SSH changes?

### `Postgres 16 + pgvector install`
- What exact DB/role names and password policy should be used?
- Should Postgres remain local-only via Unix socket + `127.0.0.1` as specified?
- Where should DB credentials be stored before Infisical is live?

### `Tenant schema + RLS migration`
- Where is the authoritative `infra/schema.sql`?
- What are the four tenant UUIDs for Pure Peptide, AgereSciences, SRU, and Twlv20 Internal?
- What tables are in Phase 1 scope beyond run-log/tenant scaffolding?
- Who approves the RLS isolation test output?

### `Infisical — self-hosted install + per-tenant scoping`
- What exact domain should be used for `infisical.<domain>`?
- Who holds the offline encryption-key backup?
- What SMTP provider/settings should Infisical use?
- Should Redis be local apt install, Docker service, or managed separately?
- Which current secrets are in-scope for migration on day one?

### `Weekly encrypted Postgres snapshots → S3 / Backblaze B2`
- Backblaze B2 or S3: which target is approved?
- What bucket name/region/lifecycle policy should be used?
- Who owns the offline GPG passphrase copy?
- What email address should receive backup failure alerts?
- What scratch host should be used for the restore drill?

### `GitHub repo scaffold + CI + deploy hook`
- Does `Twlv20-AI/twlv20-ai-skills` already exist and should this workspace be replaced by that repo, or should we scaffold here and push?
- What GitHub org/repo permissions are available?
- What runtime/service name should deploy restart via systemd?
- What test/lint stack is preferred: Node/TypeScript, Python, or mixed?
- Should deploy run directly over SSH from GitHub Actions, or should the droplet pull on webhook?

## E. Recommended execution order

No CAN DO execution set exists yet. Once blockers are cleared, the dependency-aware order should be:

1. Resolve/secure `Droplet Access` — confirm canonical IP, rotate exposed credentials, install named SSH keys.
2. Execute `Droplet hardening` — Caddy/TLS, SSH-key-only, fail2ban, UFW, unattended upgrades.
3. Execute `Postgres 16 + pgvector install` — local-only Postgres foundation.
4. Create/apply `Tenant schema + RLS migration` — after `infra/schema.sql` exists and tenants are confirmed.
5. Execute `Infisical — self-hosted install + per-tenant scoping` — depends on hardened host and DB/Redis.
6. Execute `Weekly encrypted Postgres snapshots` — depends on Postgres and Infisical secret storage.
7. Execute `GitHub repo scaffold + CI + deploy hook` — can begin in parallel once repo/access decisions are resolved, but no-op deploy depends on hardened SSH/service details.

## F. Next 3 concrete actions requiring approval before work starts

1. **Approve a credential-safety pass:** confirm the canonical droplet IP, then rotate/remove the exposed root/password material from ClickUp and replace it with named SSH-key access plus secret storage.
2. **Approve creation of missing repo structure:** add `skills/`, `specs/`, `docs/RUNBOOKS/`, and `infra/` scaffolds so the ClickUp tasks have concrete local artifacts to reference.
3. **Approve a production hardening window:** once SSH keys/domain/DNS are confirmed, execute only the `Droplet hardening` task first and capture proof-of-completion before touching Postgres/Infisical/backups.

## Raw task inventory

| id | task | status | priority | assignees | due | tags | subtasks | comments | attachments | url |
|---|---|---|---|---|---|---|---:|---:|---:|---|
| `86b9t4c3m` | `02 — Infra Build (Keeper) — Hardened checklist` | to do | none | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9t4c3m |
| `86b9qbguf` | `Droplet Access` | to do | none | none | none | none | 0 | 1 | 0 | https://app.clickup.com/t/86b9qbguf |
| `86b9m4z8h` | `GitHub repo scaffold + CI + deploy hook (twlv20-ai-skills)` | to do | high | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9m4z8h |
| `86b9m4z7r` | `Weekly encrypted Postgres snapshots → S3 / Backblaze B2` | to do | high | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9m4z7r |
| `86b9m4z6q` | `Infisical — self-hosted install + per-tenant scoping` | to do | high | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9m4z6q |
| `86b9m4z5v` | `Tenant schema + RLS migration (per Plinko spec)` | to do | urgent | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9m4z5v |
| `86b9m4z55` | `Postgres 16 + pgvector install` | to do | urgent | none | none | none | 0 | 0 | 0 | https://app.clickup.com/t/86b9m4z55 |
| `86b9m4z4w` | `Droplet hardening — Caddy/TLS, SSH-key-only, fail2ban` | to do | urgent | none | none | none | 0 | 1 | 0 | https://app.clickup.com/t/86b9m4z4w |


---

## Progress update — 2026-05-09 re-pull

Re-pulled ClickUp list `901415860610` and task comments. Jonathan's visible execution-order markers in comments appear to be:

1. `Postgres 16 + pgvector install` (`86b9m4z55`) — comment: `@Keeper John 1`
2. `Tenant schema + RLS migration (per Plinko spec)` (`86b9m4z5v`) — comment: `@Keeper John 2`
3. `Weekly encrypted Postgres snapshots → S3 / Backblaze B2` (`86b9m4z7r`) — comment: `@Keeper John 3`
4. `GitHub repo scaffold + CI + deploy hook (twlv20-ai-skills)` (`86b9m4z8h`) — comment: `@Keeper John 4`
5. `Infisical — self-hosted install + per-tenant scoping` (`86b9m4z6q`) — comment: `@Keeper John 5`
6. `Droplet hardening — Caddy/TLS, SSH-key-only, fail2ban` (`86b9m4z4w`) — comment: `@Keeper John 6`; additional comment confirms droplet IP `137.184.27.61`.
7. `02 — Infra Build (Keeper) — Hardened checklist` (`86b9t4c3m`) — comment: `@Keeper John 7`

`Droplet Access` (`86b9qbguf`) has no numeric execution-order marker in comments and remains an access/credential handoff task rather than an ordered build step.

Immediate security flag: `Droplet Access` still contains exposed credential material in the task description. Do not rotate without approval, but this should be removed/rotated as a priority.

Current step: Task #1 in Jonathan's order is `Postgres 16 + pgvector install`. It touches the production droplet and secrets, so no changes should be made without Jessie approval. The task description itself says it depends on droplet hardening, but Jonathan's order puts it first; per instruction, do not reorder or skip ahead.

### Task #1 attempt — 2026-05-09

Task: `Postgres 16 + pgvector install` (`86b9m4z55`). Jessie approved execution.

Status: **blocked before changes**.

Actions attempted:

- Tried SSH to confirmed droplet `137.184.27.61` as root with batch/key auth and `StrictHostKeyChecking=accept-new`.
- Checked basic network reachability to `137.184.27.61`.

Result:

- SSH to port 22 timed out.
- Ping received 0/3 replies.
- TCP checks to ports 22, 80, and 443 all timed out.

No production changes were made. No secrets were rotated or modified.

Blocker: The confirmed droplet IP `137.184.27.61` is not reachable from this OpenClaw host right now. Need Jessie/Jonathan/Keeper to confirm current IP, firewall/network allowlist, droplet power/status, or whether access must go through a different tunnel/VPN/node.

Next task in Jonathan's order after this one remains: `Tenant schema + RLS migration (per Plinko spec)` (`86b9m4z5v`), but per instruction I will not continue or skip ahead while Task #1 is blocked.

### Task #1 completed — 2026-05-09

Task: `Postgres 16 + pgvector install` (`86b9m4z55`). Corrected droplet IP from Jessie: `162.243.252.92`.

Status: **done**.

Changes made on production droplet:

- Added PGDG apt repository for Ubuntu 24.04 (`noble-pgdg`).
- Installed:
  - `postgresql-16`
  - `postgresql-client-16`
  - `postgresql-16-pgvector`
- Generated an app password for `twlv20_app` and stored interim copy at `/root/twlv20-secrets/twlv20_app_postgres_password` with restrictive root-only permissions. This still needs migration into Infisical/password manager per the task.
- Created/updated Postgres role `twlv20_app` with:
  - `LOGIN`
  - `NOSUPERUSER`
  - `NOCREATEDB`
  - `NOCREATEROLE`
  - `NOBYPASSRLS`
- Created database `twlv20` owned by `twlv20_app`.
- Enabled extensions in `twlv20`:
  - `vector`
  - `pgcrypto`
- Replaced `pg_hba.conf` with local-only rules:
  - local postgres peer
  - local all scram-sha-256
  - host `127.0.0.1/32` scram-sha-256
  - host `::1/128` scram-sha-256
  - no remote host rules
- Set Postgres listener to local-only (`localhost`, Unix socket `/var/run/postgresql`).
- Added tuning file `/etc/postgresql/16/main/conf.d/twlv20-tuning.conf` and restarted Postgres.

Acceptance evidence:

- `psql -V` reports `psql (PostgreSQL) 16.13`.
- `vector` and `pgcrypto` extensions are installed in `twlv20`.
- `ss -tlnp | grep 5432` shows Postgres bound only to `127.0.0.1:5432` and `[::1]:5432`.
- External TCP check from this OpenClaw host to `162.243.252.92:5432` returned `Connection refused`.
- `pg_hba.conf` has no non-localhost host rules.
- `twlv20_app` role exists and reports `rolsuper=false`, `rolcreatedb=false`, `rolcreaterole=false`, `rolbypassrls=false`.
- Tuning values verified:
  - `shared_buffers = 4GB`
  - `effective_cache_size = 8GB`
  - `work_mem = 32MB`
- pgvector smoke test as `twlv20_app` returned expected ordered rows and table was dropped.
- `postgresql@16-main` service is active.

Notes / follow-up:

- The app password is currently in an interim root-only file pending Infisical/password-manager migration. Do not expose it in chat or reports.
- There are 6 OS packages upgradable on the droplet; not touched because outside this task.
- Exposed credential material remains in ClickUp `Droplet Access`; still needs cleanup/rotation approval.

Next task in Jonathan's order: `Tenant schema + RLS migration (per Plinko spec)` (`86b9m4z5v`). This will require an authoritative `infra/schema.sql` / migration artifact and confirmation before production DB schema changes.

### Task #2 draft artifact — 2026-05-09

Task: `Tenant schema + RLS migration (per Plinko spec)` (`86b9m4z5v`). Jessie confirmed `twlv20-ai-skills` exists but `infra/schema.sql` is missing, then approved drafting locally.

Status: **drafted locally; not applied to production**.

Local files created:

- `infra/schema.sql`
- `infra/tests/rls_isolation.sql`
- `specs/tenant-schema-rls.md`
- Existing directories also now include `infra/`, `infra/tests/`, `specs/`, and `docs/RUNBOOKS/` scaffold.

Draft contents:

- Seeds four tenants: Pure Peptide Solutions, AgereSciences, Sales Recruiting University, Twlv20 Internal.
- Creates helper functions: `app_current_tenant_id()`, `touch_updated_at()`.
- Creates tenant-scoped tables: `runs`, `artifacts`, `approvals`, `tenant_secrets_refs`.
- Enables and forces RLS on all tenant-scoped tables.
- Adds tenant isolation policies keyed to `current_setting('app.tenant_id', true)`.
- Grants intended access to `twlv20_app`.
- Adds RLS pairwise isolation test and fail-closed empty-tenant check.

Validation performed:

- Copied draft to droplet and applied it only to temporary database `twlv20_schema_check`.
- Patched the test to use `twlv20_schema_check` for validation only.
- Ran `infra/tests/rls_isolation.sql` against the temporary database.
- Result: `RLS isolation test passed`.
- Dropped temporary database afterward.

No production `twlv20` schema changes were made for Task #2 yet. Production application still requires approval/review.

### Task #2 completed — 2026-05-09

Task: `Tenant schema + RLS migration (per Plinko spec)` (`86b9m4z5v`). Jessie approved production application after local draft/validation.

Status: **done**.

Changes made on production DB `twlv20`:

- Applied `infra/schema.sql`.
- Created/confirmed extensions: `pgcrypto`, `vector`.
- Created/confirmed tables:
  - `tenants`
  - `runs`
  - `artifacts`
  - `approvals`
  - `tenant_secrets_refs`
- Seeded four tenants:
  - `agere-sciences` — AgereSciences
  - `pure-peptide` — Pure Peptide Solutions
  - `sru` — Sales Recruiting University
  - `twlv20-internal` — Twlv20 Internal
- Created helper functions:
  - `app_current_tenant_id()`
  - `touch_updated_at()`
- Added update timestamp triggers.
- Enabled and forced RLS on tenant-scoped tables.
- Created tenant isolation policies:
  - `tenant_isolation_runs`
  - `tenant_isolation_artifacts`
  - `tenant_isolation_approvals`
  - `tenant_isolation_secret_refs`
- Granted intended table access to `twlv20_app`.

Acceptance evidence:

- `infra/tests/rls_isolation.sql` was run against production `twlv20` and returned: `RLS isolation test passed`.
- RLS status verified:
  - `approvals:true:true`
  - `artifacts:true:true`
  - `runs:true:true`
  - `tenant_secrets_refs:true:true`
- Policies verified in `pg_policies`.

Notes:

- The RLS isolation test wraps inserted rows in a transaction and rolls back, so no test rows remain.
- First evidence query after the test had a shell quoting error, but it occurred after schema application and test success. A corrected evidence query was run successfully.

Next task in Jonathan's order: `Weekly encrypted Postgres snapshots → S3 / Backblaze B2` (`86b9m4z7r`). This touches backups/secrets/external storage and needs B2/S3 target credentials, GPG passphrase custody, and alert email/SMTP decisions before execution.

### Task #3 completed — 2026-05-11

Task: `Weekly encrypted Postgres snapshots → S3 / Backblaze B2` (`86b9m4z7r`). Jessie approved the plan and provided B2 application credentials plus a generated GPG passphrase. Secret values were not committed to the repo or repeated in docs.

Status: **done**.

Changes made on production droplet `twlv20-prod`:

- Created root-only secret directory `/root/.secrets`.
- Stored B2 backup environment at `/root/.secrets/b2-backup.env` with mode `600`.
- Stored GPG symmetric passphrase at `/root/.secrets/pg-backup-gpg.pass` with mode `600`.
- Added backup helper `/usr/local/sbin/twlv20-pg-b2.py`.
- Added backup script `/usr/local/sbin/twlv20-postgres-backup`.
- Added failure alert script `/usr/local/sbin/twlv20-postgres-backup-alert`.
- Created/enabled systemd units:
  - `twlv20-postgres-backup.service`
  - `twlv20-postgres-backup.timer`
  - `twlv20-postgres-backup-alert@.service`
- Installed Postfix + `bsd-mailx` for local mail/sendmail failure alert delivery to `ai@twlv20.com`.
- Created/used Backblaze B2 bucket `twlv20-postgres-backups`.
- Uploaded encrypted snapshot object:
  - `postgres/weekly/2026-05-11/weekly-postgres-2026-05-11-2336UTC.sql.gz.gpg`
- Implemented retention in the backup script: keep newest 8 encrypted weekly objects under `postgres/weekly/`, delete older ones.

Acceptance evidence:

- Manual backup completed and uploaded to B2.
- End-to-end restore test succeeded:
  - downloaded latest B2 object
  - decrypted with GPG AES256 symmetric passphrase file
  - decompressed SQL
  - restored into throwaway DB `twlv20_restore_test`
  - verified `tenants` count = `4`
  - verified `runs` count = `0`
  - verified extensions `pgcrypto` and `vector`
  - dropped throwaway DB
- Timer evidence:
  - `twlv20-postgres-backup.timer` is enabled and active.
  - Next run: `Sun 2026-05-17 02:00:00 UTC`.
- Secret file evidence:
  - `/root/.secrets/b2-backup.env` mode `600`
  - `/root/.secrets/pg-backup-gpg.pass` mode `600`

Documentation added:

- `docs/RUNBOOKS/postgres-backups.md` covering manual snapshots, restore steps, secret locations, B2 key rotation, retention, and failure alerts.

Notes:

- The first manual backup attempt failed before upload because the B2 environment variables were sourced but not exported. The script was corrected to export the env before invoking the B2 helper, then the backup and restore drill succeeded.
- The B2 application key Jessie created was described as all-buckets read/write. The backup script uses only `twlv20-postgres-backups`; rotate to a bucket-scoped key when convenient.

### Task #4 partial/completed with branch-protection blocker — 2026-05-11

Task: `GitHub repo scaffold + CI + deploy hook (twlv20-ai-skills)` (`86b9m4z8h`). Jessie approved execution.

Status: **functionally deployed; branch protection blocked by GitHub plan/visibility constraints**.

Changes made in GitHub repo `Twlv20-AI/twlv20-ai-skills`:

- Repo visibility changed from public to private to match the task spec.
- Scaffolded repo layout:
  - `.github/workflows/ci.yml`
  - `.github/workflows/deploy.yml`
  - `infra/schema.sql`
  - `infra/tests/rls_isolation.sql`
  - `runtime/` Node/TypeScript scaffold
  - `skills/global/`
  - `skills/pure-peptide/`
  - `skills/agere-sciences/`
  - `skills/sru/`
  - `skills/twlv20-internal/`
  - `docs/ARCHITECTURE.md`
  - `docs/RUNBOOKS/postgres-backups.md`
  - `specs/tenant-schema-rls.md`
  - `README.md`
  - `.gitignore`
- Added CI workflow with runtime lint/build/test and schema smoke checks.
- Added deploy workflow that SSHes to the production droplet on push to `main`, pulls latest, builds runtime, and restarts systemd service.

Changes made on production droplet `twlv20-prod`:

- Created system user `twlv20-deploy`.
- Installed GitHub Actions SSH public key in `/home/twlv20-deploy/.ssh/authorized_keys`.
- Installed read-only GitHub deploy key for droplet pull access.
- Added restricted sudoers file `/etc/sudoers.d/twlv20-deploy-runtime` allowing only restart/status of `twlv20-ai-runtime.service`.
- Created `/etc/systemd/system/twlv20-ai-runtime.service`.
- Cloned repo to `/opt/twlv20-ai-skills`.
- Built and started runtime scaffold service.

Acceptance evidence:

- Local runtime validation passed:
  - `npm run build`
  - `npm test` with 2 passing tests
- GitHub Actions successful run on `main`:
  - CI: success for commit `ce5180a`
  - Deploy: success for commit `ce5180a`
- Production droplet evidence:
  - `/opt/twlv20-ai-skills` is at commit `ce5180a`
  - `twlv20-ai-runtime.service` is enabled
  - `twlv20-ai-runtime.service` is active/running
  - service logs show runtime health payload for tenants: global, pure-peptide, agere-sciences, sru, twlv20-internal
- GitHub secrets configured:
  - `DROPLET_HOST`
  - `DROPLET_USER`
  - `DROPLET_SSH_KEY`
- Repo deploy key added:
  - `twlv20-prod read-only deploy key`

Blocker:

- Branch protection could not be enabled after making the repo private. GitHub API returned: `Upgrade to GitHub Pro or make this repository public to enable this feature. (HTTP 403)`. This means the branch protection acceptance criterion is blocked by GitHub plan/repo visibility, not by implementation.

Notes:

- The first deploy run failed because `/opt/twlv20-ai-skills` had untracked files that blocked `git pull`. The deploy workflow was hardened to fetch/reset/clean.
- The second deploy run failed because `sudo systemctl status --no-pager --full` exceeded the restricted sudoers command. The workflow was corrected to use non-interactive sudo with the exact allowed systemctl commands.
- The runtime scaffold was changed to stay alive as a no-op service so systemd can keep it active until the real runtime loop lands.

### Task #5 partial — Infisical foundation installed with public URL/project setup blocked — 2026-05-12

Task: `Infisical — self-hosted install + per-tenant scoping` (`86b9m4z6q`). Jessie approved proceeding after Task #4.

Status: **partially complete; blocked on domain/Caddy, SMTP, admin/project setup, and offline key backup**.

Changes made on production droplet `twlv20-prod`:

- Installed Docker and Docker Compose v2.
- Installed Redis via apt.
- Configured Redis:
  - bound to `127.0.0.1 ::1`
  - password-protected
  - enabled via systemd
- Created Postgres role `infisical_app` with:
  - `NOSUPERUSER`
  - `NOCREATEDB`
  - `NOCREATEROLE`
  - `NOBYPASSRLS`
- Created Postgres database `infisical` owned by `infisical_app`.
- Created `/etc/infisical/.env` as root-only mode `600`.
- Created `/opt/infisical/docker-compose.yml` running `infisical/infisical:latest` as backend-only with host networking so it can reach localhost-only Postgres/Redis.
- Started container `infisical-backend`.
- Added runbook `docs/RUNBOOKS/infisical.md`.

Acceptance evidence collected:

- Docker: `Docker version 29.1.3`.
- Docker Compose: `Docker Compose version 2.40.3`.
- Redis: `redis-server v=7.0.15`.
- `infisical_app` role verified as non-superuser/non-createdb/non-createrole/non-bypassrls.
- `infisical` DB owner verified as `infisical_app`.
- Ports verified local-only:
  - Postgres on `127.0.0.1:5432` and `[::1]:5432`
  - Redis on `127.0.0.1:6379` and `[::1]:6379`
  - Infisical on `127.0.0.1:8080`
- Infisical `/api/status` returned:
  - `message: Ok`
  - `redisConfigured: true`
  - `emailConfigured: false`
- Infisical logs show Postgres and Redis connected successfully and admin signup available locally at `http://127.0.0.1:8080/admin/signup`.

Blockers / not completed yet:

- Public URL/TLS is blocked because Caddy is not installed/configured yet and no `infisical.<domain>` DNS target has been confirmed in this session.
- SMTP/email is not configured; Infisical reports `emailConfigured: false`.
- Admin account, projects, machine identities, and cross-tenant secret access checks are not complete because the instance needs first-admin setup and project creation through the UI/API.
- Offline backup of `/etc/infisical/.env` / `ENCRYPTION_KEY` still needs Jessie to confirm the offline password-manager/paper custody location before real secrets are stored.
- No real client/API secrets were migrated into Infisical yet. This is intentional until offline key backup + admin/project/machine identity setup are confirmed.

Notes:

- Initial Infisical boot failed because generated Postgres and Redis passwords contained URI-special characters. Both were rotated to URL-safe hex values, after which Infisical started cleanly.
- Current local admin signup access can be reached by SSH tunnel: `ssh -N -L 8080:127.0.0.1:8080 root@162.243.252.92`, then open `http://127.0.0.1:8080/admin/signup`.

### Task #6 completed with DNS/TLS routing deferred — 2026-05-12

Task: `Droplet hardening — Caddy/TLS, SSH-key-only, fail2ban` (`86b9m4z4w`). Jessie approved hardening.

Status: **hardening complete; public TLS host routing deferred pending domain/DNS confirmation**.

Changes made on production droplet `twlv20-prod`:

- Installed Caddy from the official Caddy apt repository.
- Added baseline `/etc/caddy/Caddyfile` with HTTP health response and JSON access logging.
- Enabled and started Caddy.
- Configured UFW:
  - default deny incoming
  - default allow outgoing
  - `22/tcp` limit
  - `80/tcp` allow
  - `443/tcp` allow
- Hardened SSH:
  - root login disabled
  - password auth disabled
  - keyboard-interactive auth disabled
  - pubkey auth enabled
  - allowed users: `keeper`, `jon`, `twlv20-deploy`
- Corrected cloud-init SSH override `/etc/ssh/sshd_config.d/50-cloud-init.conf` to `PasswordAuthentication no` so effective `sshd -T` shows password auth disabled.
- Locked root password.
- Confirmed `keeper` and `jon` key logins work and both can sudo non-interactively.
- Configured fail2ban jails:
  - `sshd`
  - `caddy-status`
- Enabled unattended upgrades.
- Added hardening runbook `docs/RUNBOOKS/droplet-hardening.md`.

Acceptance evidence:

- `root@162.243.252.92` SSH denied with `Permission denied (publickey)`.
- `keeper@162.243.252.92` SSH succeeds.
- `jon@162.243.252.92` SSH succeeds.
- Effective SSH config:
  - `permitrootlogin no`
  - `passwordauthentication no`
  - `pubkeyauthentication yes`
  - `kbdinteractiveauthentication no`
  - `allowusers keeper jon twlv20-deploy`
- UFW is active with expected rules for 22/80/443.
- Services active:
  - `caddy`
  - `fail2ban`
  - `ssh`
  - `unattended-upgrades`
- fail2ban active jails: `sshd`, `caddy-status`.
- `sshd` jail already banned multiple malicious IPs after enablement.
- Caddy responds locally: `twlv20-prod Caddy online. Public host routing pending DNS/domain confirmation.`

Deferred / blocked:

- Actual automatic TLS and host-based Caddy routing for `paperclip.<domain>`, `claw.<domain>`, and `infisical.<domain>` require confirmed domain/DNS records pointing at `162.243.252.92`.
- Current Caddy baseline intentionally does not expose local Infisical publicly until DNS/domain is confirmed.

Notes:

- Initial Caddy reload hung because the baseline config disabled the admin API and Caddy log ownership was wrong. Access was preserved via `keeper`; log ownership was corrected and Caddy was restarted cleanly.
