# 2026-05-05

- GitHub CLI was installed locally at `.local/bin/gh` because elevated apt install was unavailable from webchat.
- Authenticated GitHub CLI as `Twlv20-AI` with HTTPS protocol. Token scopes shown by gh: `repo`, `workflow`, `read:org`, `gist`.
- Jessie asked to read `https://github.com/Twlv20-AI/twlv20-ai-skills`, extract identity/purpose, and reconstruct the workspace for reliability.
- The repo contained two Word documents: `Copy of Twlv20_AI_OS___Architecture_One-Pager_v1.md.docx` and `Infra Build Steps for Phase 2.docx`.
- Extracted readable text copies into `sources/` and distilled durable workspace files: `IDENTITY.md`, `USER.md`, `PURPOSE.md`, `MEMORY.md`, and updated `AGENTS.md`.
